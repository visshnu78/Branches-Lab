# Server Configuration
PORT=5000
NODE_ENV=development

# Database (SQLite for local zero-config, PostgreSQL for production)
DATABASE_URL="file:./dev.db"

# JWT Authentication
JWT_SECRET="branches_lab_super_secret_jwt_key_2026_change_in_production"
JWT_EXPIRES_IN="7d"

# CORS Configuration
CORS_ORIGIN="http://localhost:5000,http://127.0.0.1:5500,http://localhost:3000"
